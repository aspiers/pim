#!/bin/bash

# if ! groups | grep -q systemd-journal; then
#     echo >&2 "Need to be in systemd-journal group; aborting!"
#     exit 1
# fi

jcfuu 'auto-*' -n 1000 | \
    lnavf \
        -f $0.filters \
        "$@"
